/**
 * @deprecated Unused. Slated for deletion via scripts/cleanup-dead-code.sh.
 * Stubbed to zero so it can't accidentally bloat a bundle if re-imported.
 */
export function TrendingBar() {
  return null;
}
